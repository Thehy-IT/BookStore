# Tệp cấu hình môi trường và thông tin nhạy cảm
# Không bao giờ đưa các tệp chứa thông tin kết nối cơ sở dữ liệu, API keys,
# hoặc các thông tin bí mật khác lên Git.
# Thay vào đó, bạn có thể tạo một tệp mẫu (ví dụ: config.example.php)
# và thêm tệp cấu hình thật (config.php) vào đây để bỏ qua.
config.php
db_connect.php
.env
.env.local
.env.*.local

# Thư mục chứa các gói thư viện của Composer
# Các thư viện này nên được cài đặt lại trên mỗi môi trường bằng lệnh `composer install`
/vendor/

# Thư mục chứa các gói thư viện của Node.js (npm/yarn)
# Các thư viện này nên được cài đặt lại bằng `npm install` hoặc `yarn install`
/node_modules/

# Các tệp log
# Tệp log thường chỉ dành riêng cho môi trường phát triển hoặc sản phẩm
# và không nên được quản lý phiên bản.
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Các tệp do hệ điều hành hoặc trình soạn thảo mã nguồn tạo ra
.DS_Store
Thumbs.db

# Cấu hình của các IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
